// 여러 레이어(검증/제출/페이지)에서 중복되던 사용자 메시지를 한 곳에 모은다.
// 문구를 다듬을 때 한 곳만 고치면 모든 화면이 같은 문장을 쓰도록 보장한다.
// (Zod 스키마 안의 필드별 메시지까지 전부 옮기진 않고, 실제로 중복되는 것만 상수화한다.)
export const ENROLLMENT_MESSAGES = {
  // 강의 미선택(검증/페이지/강의 가드에서 공통 사용).
  courseRequired: '수강할 강의를 선택해 주세요.',
  // 약관 미동의(검증/제출 훅에서 공통 사용).
  termsRequired: '이용약관에 동의해야 제출할 수 있습니다.',
  // 복구된 draft의 강의가 현재 신청 불가일 때의 안내.
  recoveredCourseUnavailable:
    '저장된 강의를 현재 신청할 수 없어 다시 선택해 주세요.',
} as const;
